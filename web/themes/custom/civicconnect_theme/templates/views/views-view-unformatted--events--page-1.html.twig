{% if title %}
  <h3>{{ title }}</h3>
{% endif %}

<div class="events-grid">
  {% for row in rows %}
    <div{{ row.attributes.addClass('events-grid__item') }}>
      {{ row.content }}
    </div>
  {% endfor %}
</div>
